/**
 * This lesson covers:
 * - Installing TypeScript using npm globally.
 * - Creating a `.ts` file.
 * - Writing basic TS code with static error checking.
 * - Using the `tsc` command to transpile `.ts` to `.js`.
 * - Result: clean JavaScript code ready for the browser.
 */


//tsc index.ts


console.log(Math.round(10.7));
