/**
 * This lesson explains:
 * - Creating a tsconfig.json file using `tsc --init`
 * - Organizing the project into src (source) and dist (output)
 * - Setting rootDir and outDir in the config
 * - Enabling removeComments and sourceMap
 * - Using `tsc -w` to watch all TS files for changes
 */
